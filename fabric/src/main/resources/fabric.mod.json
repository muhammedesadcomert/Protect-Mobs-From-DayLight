{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "${authors}"
  ],
  "contact": {
    "homepage": "${homepage}",
    "issues": "${issues}"
  },
  "license": "${license}",
  "icon": "${icon}",
  "environment": "*",
  "entrypoints": {
    "main": [
      "com.muhammedesadcomert.protectmobsfromdaylight.fabric.ProtectMobsFromDayLight"
    ]
  },
  "mixins": [
    "fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${fabric_loader}",
    "fabric-api": "*",
    "minecraft": "~${minecraft}",
    "java": ">=17"
  },
  "suggests": {
    "another-mod": "*"
  }
}
